name: bmi_calc_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  animated_splash_screen: ^1.3.0
  curved_labeled_navigation_bar: ^2.0.2
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter_icons:
  image_path: "images/bmi.png"
  android: true
  ios: true
flutter:
  uses-material-design: true
  assets:
    - images/
